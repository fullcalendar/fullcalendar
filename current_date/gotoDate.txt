
gotoDate
========

Moves the calendar to an arbitrary date.

<div class='spec' markdown='1'>
.fullCalendar( 'gotoDate', date )
</div>

`date` can be a [Moment](../utilities/Moment) object, or anything the Moment constructor accepts.